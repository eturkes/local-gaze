@AGENTS.md

# Claude Code
- Adapt `/session-prompt` + `/codex-review` to the project as it evolves; keep them token-efficient + agent-only (skip human UX / usage hints). I trigger them.
- I usually cap you at 200K context — it sharpens you but demands token discipline: prune redundant/obsolete info + structures often.
- Past 80% context (watch via `.agent/context.sh`): drive ongoing work to a clean state, hold new tasks, and close out cleanly when you confidently can. Otherwise I compact manually, then direct you to continue from where you halted or to revert and retry at smaller scope.
- do-not-read set = paths not worth an agent's tokens, distinct from `.gitignore` (some committed files aren't worth reading, some gitignored ones are). Enforce + keep synced on two fronts: `.serena/project.yml` `ignored_paths` (committed → sessions/subagents inherit) for the *non-gitignored* paths, since Serena already honors `.gitignore`; `permissions.deny` `Read()` (`.claude/settings.json`) for *all* of them, since `Read`/`Bash` bypass `.gitignore`.
- Deny-`Read()` globs (`permissions.deny`) = gitignore-style, silently over-/under-matching → never trust by eye. `dir/*/**` over-denies: `*` = one segment incl. a bare filename + `/**` = zero+ segments → also blocks `dir/child`, not just `dir/sub/deep`; prefer `**/*.ext` (binary/dump trees) + exact-path rules. Anchors: leading `/` = project root, `//` = fs-absolute, bare name = any depth. Rules hot-reload → after each edit Read-test one path that MUST block + one that MUST stay readable (catches over- and under-deny). A `Read()` deny also gates `Bash`, leakily, via a static check on the head command's identity (not the RTK wrapper — `grep`+`find` are both RTK shell-fns, yet `grep` blocks and `find` doesn't): recognized readers naming a matching path are blocked (`grep`/`stat`/`jq`) while others slip (`ls`/`wc`/`echo`, and `find` though it's a filesystem reader like `stat`) → to read a denied file deliberately, use a slipping cmd (`find` to list denied trees) or runtime indirection (a script/module that `open()`s it, path never in the command).
